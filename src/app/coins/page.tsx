import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Coins | eFootball Nexon",
};

export default function CoinsPage() {
  return <main className="page-shell" aria-label="Coins" />;
}
